import 'package:flutter/material.dart';

class HelpRequestFormPage extends StatelessWidget {
  const HelpRequestFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Post Help Request'),
      ),
      body: const Center(
        child: Text('Help Request Form (Coming Soon)'),
      ),
    );
  }
}
